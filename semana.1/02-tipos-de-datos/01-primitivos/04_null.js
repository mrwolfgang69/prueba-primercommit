/*********************************/
/* TIPOS DE DATOS ==> #4 NULL   */
/********************************/
// El valor especial null no pertenece a ninguno de los tipos descritos anteriormente. Forma un tipo propio separado que contiene sólo el valor null. 

let nada = null // guarda un espacio en la memoria, diéndole que ahí hay algo.

console.log(typeof nada) // ==> Esto está oficialmente reconocido como un error de comportamiento de typeof que proviene de los primeros días de JS y se mantiene por compatibilidad.